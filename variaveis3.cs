using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class variaveis3 : MonoBehaviour
{

    /*
    
    == igual
    != diferente
    > maior que
    < menor que
    >= maior ou igual a
    <= menor ou igual a

    */

    int numero1 = 10;
    int numero2 = 20;

    // Start is called before the first frame update
    void Start()
    {
        /*
        print(numero1 == numero2 ? " AEE " : " AAA ");
        print(numero1 != numero2 ? " AEE " : " AAA ");
        print(numero1 > numero2);
        print(numero1 < numero2);
        print(numero1 >= numero2);
        print(numero1 <= numero2);
        */

     //   print(numero1 != numero2 && numero1 < numero2); //print true
        /*
            && - E
            || - OU
            !  - Não (ao contrário)
          
         */

       // print(!(numero1 != numero2 && numero1 < numero2)); //print false por conta do ! no início

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
